export interface SlideData {
  id: number;
  image: string; // Ruta de la imagen
  title: string;
  subtitle: string;
  dateText: string;
  ctaText: string;
  ctaLink: string;
  bgColor: string; // Color de fondo vibrante para la caja de texto (hex)
}
